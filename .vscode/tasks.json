{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "nvm use 20 && pnpm run start:dev",
      "type": "shell",
      "command": "[ -s \"$HOME/.nvm/nvm.sh\" ] && . \"$HOME/.nvm/nvm.sh\"; nvm use 20 && pnpm run start:dev",
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      },
      "options": {
        "shell": {
          "executable": "bash",
          "args": ["-c"]
        }
      }
    }
  ]
}
